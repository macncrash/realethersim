import * as THREE from 'three';

// High-contrast dark mode (FR-2.3): deep near-black background maximizes trail/line visibility.
export const theme = {
  background: new THREE.Color(0x05070d),
  accent: new THREE.Color(0x4ad6c8),
};
